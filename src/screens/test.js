import * as React from 'react';
import { Drawer } from 'react-native-paper';

const test = () => (
   <Drawer.Item
     style={{ backgroundColor: '#64ffda' }}
     icon="star"
     label="First Item"
   />
);

export default test;